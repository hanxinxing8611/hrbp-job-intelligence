services:
  - type: web
    name: hrbp-job-intelligence
    runtime: node
    plan: free
    buildCommand: "echo 'Build completed'"
    startCommand: "node server/server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
